---
layout: page.njk
title: "Knowledge Base"
section: "Knowledge"
description: "A collection of knowledge articles and resources."
displaySidebar: false
permalink: '/kb/'
---

{% for post in collections.kb | reverse %}
  <div>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    {% if post.data.tags %}
      <div class="meta-info tags">
        {% for tag in post.data.tags %}
          <span class="tag">#{{ tag }}</span>
        {% endfor %}
      </div>
    {% endif %}
  </div>
{% endfor %}