# Rare Styles — Changelog

All notable changes to Rare Styles are documented in this file.
Format follows [Keep a Changelog](https://keepachangelog.com/). The library follows semantic versioning.

Release manifest for `v0.6.14`: [`HARVEST_v0.6.14.md`](./HARVEST_v0.6.14.md).
Roadmap and milestones: [`BACKLOG.md`](./BACKLOG.md).

---

## [Unreleased]

## [v0.6.15] — 2026-06-12 — Audit Hotfixes & Post-Harvest Cleanup

Tight follow-up patch to `v0.6.14`: clears the 2026-06-06 / 2026-06-11 audit findings and the first post-harvest fixes. Bugs, hygiene, asset-path cleanup, and two narrowly scoped harvested selectors. Nothing here was hotfixed back into `v0.6.14`.

### Added

- **`.boilerplate`** in `assets/css/modules/typography/_text-content.scss` (`CSS-059`) — a muted text-notes / news block set off from the prose by a top rule and a leading `info` marker. Structural contract: a `.boilerplate` container holding one or more `<p>` elements; the `info` glyph is drawn per paragraph via `::before` and depends on the canonical Material Symbols Outlined family. Harvested from sibling projects via the annotated `move-in.css` transfer scratchpad, which is now removed.
- **`.feature-row` / `.feature-row__item`** in `assets/css/modules/bricks/_sections.scss` (`CSS-059`) — a horizontal, overflow-scrolling row that enumerates features as equal-width cards. Structural contract: a `.feature-row` container holding one or more `.feature-row__item` children, each a vertical flex column (top media, bottom copy) with a fixed responsive width (`clamp(280px, 30vw, 420px)`) so the row scrolls rather than squashing its items. Normalized to hybrid BEM on import.

### Changed

- **normalize.css is now emitted first** (`CSS-048`). In `rare.scss`, `@use "vendor/normalize"` was ordered after `@use "modules/index"`, so the reset landed at the end of the bundle and overrode component styles instead of underpinning them. Reordered so normalize underpins the modules. The broader reset/normalization audit stays deferred to `CSS-078` (`v0.7.1`).
- **`--font-size` is now `1rem`** instead of the literal `16px` (`CSS-035`). The root font-size token now honors the user's browser font-size preference, matching every other already-rem size token.
- **`.card-grid` tile copy tightened** (`CSS-057`). Paragraphs inside `.card-grid` were inheriting generic prose rhythm and reading too loose on the compact tile surface. Added a scoped `.card-grid p` adjustment (`font-size: 0.9em`, `line-height: 1.1`, top-only margin); scoped to `.card-grid` so it does not bleed into other card patterns.
- **`.list-fixed-rows` now equalizes its implicit columns** (`CSS-056`). Added `grid-auto-columns: minmax(0, 1fr)` so every generated column shares the available width evenly instead of sizing to its own content, which previously made visually identical lists render with different column widths.
- **Library-owned vendor and brand image surfaces now resolve via package-local relative paths** (`CSS-058`). `.vendor-logo.github`, `.vendor-logo.wa` (`_media.scss`) and `.wa-button` (`_web-reusable.scss`) moved off `/assets/img/common/vendors/...` onto `images/vendors/{github,whatsapp}/...`. The Rare Digits brand logo (`.header-logo`) moved out of the plain-CSS `rare-website.css` site layer into the SCSS pipeline (`_header-container.scss`) and now resolves `images/vendors/rare-digits/rare-logo-black.svg` — the same relative-path convention as the blockquote mark, so downstream consumers no longer depend on site-root image folders. `rare-website.css` is retained as the intentional site-override layer (now effectively empty).

### Fixed

- **`CSS-027` — responsive spacing aliases were broken in production.** `_spacing-aliases.scss` used `\\:` and compiled to a literal backslash in the class name (`.mobile\\:p-s`), so every short-form responsive alias (`mobile:p-s`, `tablet:m-l`, `desktop:gap-xl`, …) failed to match HTML. Switched to `\:` to match `_spacing.scss`; verified single-backslash output in `rare.css` (zero double-backslash occurrences).
- **`CSS-028` — duplicate `--header-height`.** The token was declared in both `layout/_containers.scss` and `navigation/header/_header-container.scss`. Dropped the header-module copy; `layout/_containers.scss` is the single owner.
- **`CSS-049` — generated utilities emitted invalid CSS.** The `$spaces` matrix produced `.gap-auto`, `.gap-x-auto`, `.gap-y-auto` (`gap: auto` is not valid) in the base set and across all three responsive prefixes. `auto` is now special-cased out of every gap family. The full property×value matrix audit stays `CSS-079` (`v0.7.1`).
- **`CSS-051` — install snippets on `/styles/usage/` pointed at GitHub Pages.** Updated all three snippets to the versioned jsDelivr CDN URL (`cdn.jsdelivr.net/gh/raredigits/rare-styles@{{ versions.styles }}/...`), matching the `/styles/` landing page. The broader Pages sunset remains `CSS-T00.2` (`v0.6.17`).
- **`CSS-029` — duplicate brand class.** `.rare-brand` and `.rare-brand-color` produced identical rules. `.rare-brand-color` is now the canonical class; `.rare-brand` is kept as a documented deprecated alias until `1.0.0` (it is consumed by external projects, so it is not removed outright).

### Removed

- **`.list-fixed-rows-2col` (breaking).** The two-equal-columns modifier is removed as part of `CSS-056`: with `grid-auto-columns: minmax(0, 1fr)` now equalizing the implicit columns on the base `.list-fixed-rows`, the dedicated `-2col` variant became redundant. Author a two-column fixed-rows list by setting `--list-rows` on the base utility instead. (`.list-fixed-rows-2col` shipped only in `v0.6.14`, so this corrects it within one release.)

## [v0.6.14] — 2026-06-12 — Cross-Project Enrichment

### Added

- **`@mixin outdent`** in `assets/css/modules/typography/_text-content.scss`. Symmetric outdent pattern that pulls a block past the text-content column on both sides: `margin: 0 calc(-1 * var(--space-lg))` on desktop, `margin: 0 calc(-1 * var(--space-sm))` on mobile. Internal mixin for now; a public `.rd-outdent` utility is planned for `v0.6.15` once the `rd-` namespace lands (`CSS-060..063`).
- **`@mixin outdent`** in `assets/css/modules/typography/_text-content.scss`. Symmetric horizontal outdent pattern that pulls a block past the text-content column on both sides: `margin-left` / `margin-right` are set to `calc(-1 * var(--space-lg))` on desktop and tighten to `calc(-1 * var(--space-sm))` on mobile. Internal mixin for now; a public `.rd-outdent` utility is planned for `v0.6.15` once the `rd-` namespace lands (`CSS-060..063`).
- **`.list-columns`** in `assets/css/modules/typography/_lists.scss` — multi-column packer for a single long flat list. Defaults to two columns with `var(--grid-gap)` gap, list-style reset, and automatic collapse to one column on mobile. Column-count overrides `.list-columns-3/4/5` bump the count and step down to two on mobile. Will be re-released as `.rd-list-columns` in `v0.6.15`.
- **`.list-fixed-rows`** in `_lists.scss` — grid-based list packer driven by an explicit row count (harvested from sibling projects as `.list-grid`). The author sets `--list-rows` (namespaced like every list tuner; briefly `--rows` during the harvest pass); the browser fills columns automatically left-to-right via `grid-auto-flow: column`. Variant `.list-fixed-rows-2col` (briefly `-toc`) locks two equal columns — most commonly a table of contents — and stacks to one column on mobile. The structural `-2col` suffix replaced the use-case `-toc` suffix to keep modifier naming on one abstraction level across the list family. On mobile the base utility drops the fixed-row contract and reflows into two equal columns in row order; the count is overridable with the responsive grid utilities (`mobile:grid-cols-N`) because the mobile rule consumes the same `--grid-columns` custom property they set.
- **`.catalog-list`** in `_lists.scss` — a scannable vertical list of entries the reader browses to pick one: catalogs, knowledge-base indexes, portfolios, search results (harvested from sibling projects as `.group-list`; briefly `.directory-list` during the harvest pass — never released). Deliberately minimal API: `.catalog-list__item`, `.catalog-list__title`, `.catalog-list__meta`. The harvested `__tags` and `__description` elements were dropped as over-specification — tag chips sit inline in the meta line (`.tag` already carries its own sizing), summary paragraphs are host content the component does not own. The `.catalog-list--nest` modifier (was `--tree`) indents everything under the title, so each entry reads as a parent with its nested children/contents — directory-tree indexes, API surfaces, section maps. Renamed from the harvested `.group-list` (and its generic `.item-*` / `.tags` descendants) so the application area reads from the class name. Documented on `/styles/typography/lists/`.
- **`.table-comparison`** in `assets/css/modules/typography/_tables.scss` — preset that centers every value column under its header while the first column (row label) stays left, for feature / option comparison tables in normal body type. Harvested from sibling projects (source spelled the partner class `horisontal`; see below). Kept as an independent preset alongside `.table-terminal`: both share the "center non-first columns" alignment but deliberately keep their own rules rather than extracting a shared modifier — terminal adds the monospace font and a right-aligned last column for numeric readouts.
- **`.table-horizontal-borders`** in `_tables.scss` — lighter border preset: a quiet rule between rows plus extra vertical padding, with no outer frame or vertical lines (the middle ground between bare and `.table-bordered`). Harvested as `.table-horisontal-borders`; **shipped with the spelling corrected** to `horizontal`. The harvested `tr:hover` used `var(--white)`, which is now the single hover treatment across every table preset (see Fixed → `CSS-054`).
- **`.table-numeric`** in `_tables.scss` — preset for decision-grade figures: `font-variant-numeric: tabular-nums` (equal-width digits so numbers line up down a column) plus right-aligned value columns, row label stays left. Normal text font; structured like `.table-comparison`. Kept opt-in rather than making `tabular-nums` a global default on every `table`.
- **`.table-scroll`** wrapper in `_tables.scss` — `overflow-x: auto` on a block wrapper, the working replacement for the non-functional `overflow-x` that used to sit on the `table` element (see Fixed → `CSS-053`). Wrap a wide table to let it scroll instead of breaking the layout grid.
- **`.card-grid`** in `assets/css/modules/bricks/_cards.scss` — a self-contained tile component that arranges `.card-grid__item` tiles into a responsive grid. Column count via the `--card-grid-cols` custom property (default 5) and the `.card-grid--cols-2/3/4` modifiers; below desktop every variant collapses to two. Two tile treatments: `.card-grid--cards` (square card surfaces) and `.card-grid--gallery` (image tiles with a `.card-grid__title` overlay). Harvested from sibling projects and normalized to hybrid BEM — the collision-prone generic `.cols-N` became `.card-grid--cols-N` (`CSS-141`), and the generic descendants `.gallery-item` / `.galery-title` / `.item-title` became `.card-grid__item` / `.card-grid__title`. Bugs fixed on the way in: the `.galery-title` typo (harvest source bug 4, gone via the rename) and `padding-top: var(--shadow-md)` (a shadow token used as spacing → `var(--space-sm)`); the `@media (width <= 1023px)` range query became `bp.multi-breakpoint("mobile", "tablet")`. Documented on `/styles/bricks/`.
- **`figure.epigraph`** in `assets/css/modules/typography/_text-content.scss` — a short, right-aligned italic attributed quote for the head of a section or page (a voice from outside the prose, distinct from `blockquote`). Harvested from sibling projects; placed next to `blockquote` as a special text element rather than in its own file. The source's duplicated `margin` declaration was removed, the magic `3em` gap tokenized to `var(--space-xl)`, and `line-height: 1.3` to `var(--line-height-sm)`. Documented in the Special Text Elements section of `/styles/typography/text-content/`.
- **`.wide-background`, `.wide-section`, and `.wide-background-text-content-wrapper`** in `assets/css/modules/bricks/_sections.scss` — a full-bleed section pattern for images, context bands, and wide inserts that need to escape the normal reading column without losing inner text discipline. The outer section breaks to the viewport edge; the inner wrapper returns prose/content to the canonical track. To support the harvested breakout math, the shell now declares `--field-width: max(0px, calc(50vw - (var(--site-max-width) / 2)))` in `layout/_containers.scss`, complementing the existing `--sidebar-width` and `--grid-gap-global`. The `max(0px, …)` guard matters because the desktop breakpoint (1024px) starts below the `--site-max-width` clamp minimum (1116px): without it, the gutter went negative in the 1024–1116px window and the full-bleed block overflowed horizontally by the scrollbar width. Verified full-bleed with zero horizontal overflow across that window and above.
- **`.center`** in `assets/css/modules/align/_align.scss` — the simplest possible content-centering helper: `text-align: center` and nothing else. It complements the existing layout-oriented helpers (`.center-center`, `.center-x`, `.grid-center`) with a plain text/content alignment utility for narrow cases where introducing flex/grid behavior would be the wrong tool.
- **`.paper-sheet`** in `assets/css/modules/bricks/_cards.scss` — a paper-like content surface for document-style inserts: fixed sheet aspect ratio, white background, `var(--shadow-md)`, and generous internal padding. Harvested from sibling projects and kept narrow on purpose: not a generic card replacement, but a reusable “document on the page” treatment.
- **`.scroll-container`** in `assets/css/modules/utilities/_display.scss` — the smallest possible overflow helper: `overflow: auto` on an opt-in wrapper. Harvested from sibling projects and kept intentionally plain; it lives in the display/overflow utility layer as a neutral way to let any block scroll when needed.
- **`.iframe-video`** in `assets/css/modules/decorations/_media.scss` — responsive media wrapper for embedded video: full-width container with a fixed `16 / 9` aspect ratio whose nested `iframe` fills the box without extra border chrome.
- Documentation: `/styles/typography/tables/` extended with the new presets (`.table-comparison`, `.table-horizontal-borders`, `.table-numeric`, `.table-scroll`) — selector reference + live examples, a `.table-comparison` vs `.table-terminal` decision note, and a "table vs `.dl-grid`" rule for two-column key:value data.
- Documentation: `/styles/typography/lists/` rewritten as a task-driven guide — when to use plain `<ul>`/`<ol>`, `.list-unstyled`, `.list-title`, then the three long-list packers (`.list-columns`, `.list-group-pack`, `.list-fixed-rows`) with the decision rationale for each, plus the feed list. Library stance documented: CSS multi-column is treated primarily as a list tool, not a magazine prose-layout system.
- New documentation page `/styles/typography/tables/` — the tables content was split out of the previous `Lists and Tables` page. Sidebar (`_data/sidebarmenu.json`) and table of contents (`_data/tableOfContents.json`) updated accordingly. The old page is renamed in the UI to `Lists & Columns`; the URL `/styles/typography/lists/` is preserved to keep external links working.

### Changed

- **`.text-content-caption`, `.highlight`, `p.highlight`** now consume `@include outdent` instead of inline `margin-left` / `margin-right` declarations. Side effect: mobile `margin-right` is now symmetric with `margin-left` (`-var(--space-sm)`); previously it stayed at `-var(--space-lg)`.
- **`.caption`** now consumes `@include outdent`. Side effect: mobile breakpoint now matches `margin-left` / `margin-right` to `-var(--space-sm)`; previously `.caption` had no mobile override at all and kept `-var(--space-lg)` on both sides regardless of viewport.
- **`.list-group-pack` responsive fallbacks** (behavior change vs the released `.list-pack`): mobile now falls back to 2 balanced columns instead of 3 — three columns were too tight on phone widths; tablet gets an explicit 3-column balanced fallback instead of inheriting the height-driven desktop packing. Height utilities are reset to `auto` on both.
- **`.list-title` is now a pure label utility** (behavior change vs release): its old block-style `margin: 0 0 var(--space-md)` is dropped, and it now pins `font-family: var(--primary-font)` and `font-size: 1em` so the label stays stable on any element. The markup convention is a plain `<span class="list-title">`: the label is a visual marker, not a document heading, so the previously used `<h4>` was dropped from index-list markup (`styles/modules/table-of-contents/index.njk`, docs examples). Most bottom rhythm moved to the `.index-list` block (`margin-bottom: var(--space-md)`), which also means the block class now carries real rules instead of being a bare markup marker.
- **Page shell layout is now owned by `layout/_containers.scss`** (harvested shell patch, normalized). The repeated recipient-project intent from `html, .main-body { height: 100%; min-height: 100vh; margin: 0 }` was not imported literally: the consumer-only `.main-body` class was dropped, but its full-height shell behavior was accepted into the library as `html, body { height: 100%; min-height: 100vh; }`, `body { display: flex; flex-direction: column; ... }`, and `main { flex: 1 0 auto; ... }`. This makes the library's page shell explicit and keeps footer flow driven by the flex column rather than by `main { height: 100% }`.
- **Sidebar gains an internal-scroll affordance** (harvested patch). The desktop `.sidebar` now caps at `max-height: calc(100vh - header - space-lg)` and scrolls its own overflow with a thin, reserved-gutter scrollbar, so a long sidebar never pushes the page or shifts layout on scroll. Reset on mobile, where the sidebar is a short horizontal bar. The source's `overflow-x: hidden` was intentionally not adopted (it can clip the mobile horizontal nav).
- **`.highlight` / `p.highlight` gain `margin-bottom: var(--space-md)`** (harvested patch) — emphatic blocks now have bottom separation from the following text. Added after the `@include outdent` block so it applies to both `<div>` and `<p>` highlights.
- **Emphatic blocks inside a sidenote run wider** (harvested patch): `.sidenote-wrapper > .highlight` and `> .text-content-caption` span 6 of the 9 sidenote columns (up from the default 5), still clear of the sidenote at column 7.
- **First-child caption reset** (harvested patch): `article .text-content-caption:first-child` drops its top padding (`padding: 0 0 var(--space-sm)`) so a caption opening an article aligns to the top edge.
- **Minor style refinements** landed across a few existing shell/navigation rules, including `.sidebar`, header grid placement, and `.hamburger`, without changing the public API surface.

### Fixed

- **Blockquote no longer hardcodes a CDN URL** (`HARVEST` source bug 5 / finding F7). `typography/_text-content.scss` pinned `background-image: url('https://cdn.jsdelivr.net/gh/raredigits/rare-styles@v0.6.13/…/mark-quote.svg')` — a circular self-reference that broke offline development and needed a manual bump every release. Replaced with the relative `url('images/common/mark-quote.svg')`, which resolves both on this site (`/assets/css/…`) and in the published package (`/…`). This also surfaced that the canonical reusable-image folder `assets/css/images/**` — declared as the public asset contract in `STYLEGUIDE.md` — was never passed through to the built site; added `eleventyConfig.addPassthroughCopy("assets/css/images")` to `.eleventy.js` so the folder is actually served (also unblocks the `CSS-T00.1` vendor-logo migration).

Table bugs surfaced in the 2026-06-11 tables pass and fixed in-place during it (originally filed for `v0.6.14_1` as `CSS-053..055`; pulled forward into this release).

- **`CSS-053` — table horizontal scroll never worked.** `overflow-x: auto` sat on the `table` element, which the table layout model ignores. Removed it and shipped the opt-in `.table-scroll` wrapper instead (see Added). The `/styles/typography/tables/` copy that claimed tables auto-scroll is corrected.
- **`CSS-054` — zebra/hover direction was inverted in `.table-striped`.** Even rows were `--gray-mid-light` (#e4e4e4, darker than the page) while hover was `--gray-lightest` (lighter) — the two pulled in opposite directions. Both now go lighter: even rows are `--gray-lightest` (#fafafa) and the hover for every preset is `--white` (#fff). One consistent "lift" affordance across `.table-striped`, `.table-terminal`, `.table-bordered`, and `.table-horizontal-borders`.
- **`CSS-055` — dead declaration removed.** `thead, tbody { width: 100% }` is ignored by the table layout model; dropped.
- **`CSS-037` — last stylelint error cleared.** `layout/_grid.scss:137` flagged `at-rule-empty-line-before` on the `@if` inside `generate-col-position`; added the missing blank line. `npm run lint:css` is now clean. Build-time only — no change to the generated `.col-start-*` / `.col-end-*` classes. Originally filed for `v0.6.14_1`; pulled forward so the release ships lint-clean.

### Renamed (BREAKING)

- **`assets/css/modules/decorations/_images.scss` → `assets/css/modules/decorations/_media.scss`**. The module now owns general media primitives rather than only images, which makes room for the new `.iframe-video` wrapper alongside the existing `img` and `.vendor-logo` rules.
- **`.columns` → `.prose-columns`** in `_text-content.scss` (briefly `.text-columns` during the harvest pass — never released). The full family of generated classes is renamed in lock-step: `.columns-1..6` → `.prose-columns-1..6`, `.mobile:columns-N` → `.mobile:prose-columns-N`, same for `tablet:` and `desktop:` prefixes. No alias kept — pre-release library, no downstream consumers. The rename clarifies the prose-vs-list split now that `.list-columns` exists side by side, and names the actual use case: the rare multi-column prose block. `.prose-columns` is kept as the escape hatch but is not featured on the lists docs page. Closes `CSS-077` ahead of its `0.7.0` slot — done before the intermediate name ever shipped, so no extra breaking change. Will be re-released as `.rd-prose-columns` in `v0.6.15`.
- Internal reference updated: the rule for column-aware lists now reads `.prose-columns .list { ... }`.
- Comment reference updated: `_grid.scss` cross-reference in the auto-fit grids explanation now points at `.prose-columns`.
- Draft content updated: three `<div class="columns">` occurrences in `_drafts/new-project-workflow.md` rewritten to `.prose-columns`.
- **`.list-pack` → `.list-group-pack`** in `_lists.scss`, with its tuning variable `--list-pack-min` → `--list-group-pack-min` (briefly `.list-multigroup` / `--list-multigroup-min` during the harvest pass — never released). The final name keeps the original packing metaphor and adds what is packed: named groups that must not split across a column edge. Consumers updated: `_includes/hamburger.njk` and the inline example on `/styles/layout/spacing/` (which also had a stale `--list-pack-height` custom property — corrected to a plain `height`).
- **`.dl-inline` → `.dl-grid`** in the definition-list styles. The old name described placement ("inline" with the term), the new one describes the mechanism and result: a two-column `key: value` grid. BREAKING vs `v0.6.13` — no alias kept. Along the way `.dl-grid dt` dropped `color: var(--text-color-light)`: keys render in the normal text color and rely on layout, not muting, to read as keys. Consumers updated: the two examples on `/styles/layout/spacing/`.
- **`.index-list` normalized to BEM** — `.index-list h4` → `.index-list__title`, `.index-list ul li` → `.index-list__item`; the list reset on the `<ul>` now reuses `.list-unstyled` in markup instead of a bespoke `.index-list ul` rule. Markup updated in `styles/modules/table-of-contents/index.njk`. Brings it in line with the BEM siblings `.feed-list` / `.catalog-list`. Later in the same release both elements were retired: `__title` in favor of the shared `.list-title` utility, and `__item` in favor of the component styling its canonical inner `ul`/`li` directly (see Cleanup).

### Cleanup (consistency)

- **`.lead`** in `assets/css/modules/typography/_text-content.scss` now uses the shared `@mixin outdent`, so the typographic lead paragraph breaks out of the text column on the same rhythm as neighboring special text elements such as `.highlight`, `.text-content-caption`, and `.caption`.
- **`.index-list__title` removed in favor of `.list-title`** — the two classes rendered the same bold-uppercase label and would inevitably drift apart. Index lists now compose the shared utility; the only thing `__title` added (heading-size normalization) moved into `.list-title` as `font-size: 1em`. Markup updated: `styles/modules/table-of-contents/index.njk`, `/styles/typography/lists/` examples.
- **`.list-group-pack` is now composed of `.index-list` units** — formalized as the canonical composition: the packer arranges columns, the index lists carry the content (a `.list-title` label over a plain `ul`/`li` owned by the component) and their own bottom rhythm. The hamburger Manuals section (`_includes/hamburger.njk`) was recomposed accordingly: the anonymous `.list` wrapper divs and `.nav-list` resets are replaced with `.index-list` blocks; the `/styles/typography/lists/` group-pack example shows the same composition.
- **`.index-list__item` removed — the component styles its inner `ul`/`li` directly** via child combinators (`> ul`, `> ul > li`): the structure is canonical HTML and any `li` inside an index list is an index entry by definition, so the BEM element added markup noise, not information (36 class attributes in the hamburger alone). Markup simplified in `_includes/hamburger.njk`, `styles/modules/table-of-contents/index.njk` and the docs examples; `.list-unstyled` is no longer needed inside index lists (it stays in the library as a standalone utility). Codified rule of thumb: BEM elements are for containers whose role does not read from the tag (`div`/`span` → `__meta`, `__title`); self-describing structures (`ul > li`, `dl`, tables) are styled as elements with child combinators.
- **`.feed-list` BEM normalization** — `.feed-list__item h4` → `.feed-list__title`, `.feed-list__item-meta` → `.feed-list__meta`. The bare `h4` tag selector and the `__item-meta` grandchild name were the last non-BEM leftovers among the list components; `__meta` now mirrors `.catalog-list__meta`. Markup updated: `kb/index.njk` and the `/styles/typography/lists/` examples (`examples/index.md` uses only `.feed-list__item` and needed no change). No visual change.
- **`.nav-list`** consolidated into a single self-contained rule in `navigation/_navigation.scss` (`list-style-type: none; padding: 0; margin-bottom`). The duplicate list-reset copy in `_lists.scss` (which also duplicated `.list-unstyled`) was removed. No behavior change.
- **`.prose-columns .list`** rule moved from `_lists.scss` to `_text-content.scss`, next to the `.prose-columns` definition it depends on. Keeps `_lists.scss` purely list-focused.
- **Definition-list styles (`dl`, `dt`, `dd + dt`, `.dl-grid`) moved from `_lists.scss` to `_text-content.scss`** — a `<dl>` is a special text element (principle blocks, glossaries, key:value metadata), not a list pattern; the move completes the "purely list-focused `_lists.scss`" cleanup. Documentation moved accordingly: from `/styles/typography/lists/` to the **Special Text Elements** section of `/styles/typography/text-content/`.
- **`.list-columns`** list reset harmonized from `list-style: none` to `list-style-type: none`, matching the canonical `.list-unstyled` / `.list-fixed-rows` resets.

### Removed

- **`.lead-container`** — width-constraining wrapper for `.lead` removed as redundant. The `.lead` paragraph stands on its own and inherits the text-content width like any other paragraph.
- **`.columns`** (and the responsive-prefixed family) — replaced by `.prose-columns`. See **Renamed**.
